{
  "name": "tidal",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "dev": "pnpm -F @tidal/web dev",
    "dev:menubar": "pnpm -F @tidal/menubar dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "start": "pnpm -F @tidal/web start",
    "test:ui": "pnpm -F @tidal/web test:ui",
    "dist:menubar": "pnpm -F @tidal/menubar dist",
    "release:menubar": "git tag menubar-v$(node -p \"require('./apps/menubar/package.json').version\") && git push origin menubar-v$(node -p \"require('./apps/menubar/package.json').version\")"
  }
}
